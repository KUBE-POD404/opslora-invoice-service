apiVersion: v1
kind: Service
metadata:
  name: invoice-service
  namespace: opslora-test-ns
spec:
  type: ClusterIP
  selector:
    app: invoice-microservice
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP